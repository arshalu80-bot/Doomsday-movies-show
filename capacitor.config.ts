import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avengers.doomsdaytracker',
  appName: 'Avengers Doomsday Tracker',
  webDir: 'dist'
};

export default config;
